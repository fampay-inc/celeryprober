# Celery Monitor

An application to detect Celery task drops.


### Running the application
Execute: `just run`

### Build
Execute: `just build`
> Binary can be found in the `<ROOT>/build/` directory.
